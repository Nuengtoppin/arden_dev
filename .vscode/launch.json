{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (Arden)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/${workspaceFolderBasename}.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "check",
            "terminal": "integrated"
        }
    ]
}